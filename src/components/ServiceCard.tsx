'use client';import React,{useRef,useState}from 'react';import{motion,useInView}from 'framer-motion';import Image from 'next/image';import Link from 'next/link';interface ServiceCardProps{title:string;description:string;imageSrc:string;link:string;index:number;type:'vopsele'|'izolații';}const ServiceCard=({title,description,imageSrc,link,index,type}:ServiceCardProps)=>{const cardRef=useRef<HTMLDivElement>(null);const isInView=useInView(cardRef,{once:false,amount:0.3});const[imageError,setImageError]=useState(false);const bgColor=type==='vopsele'?'#B99C4B':'#1A1A1A';const textColor=type==='vopsele'?'#1A1A1A':'#F0E4B2';const borderColor=type==='vopsele'?'#F0E4B2':'#B99C4B';return(<motion.div ref={cardRef}className="relative rounded-2xl overflow-hidden aspect-square" initial={{opacity:0,y:50}}animate={isInView?{opacity:1,y:0}:{opacity:0,y:50}}transition={{duration:0.6,delay:index*0.1}}>{ }<div className="absolute inset-0 w-full h-full">{imageError?(<div className="w-full h-full flex items-center justify-center text-center p-4" style={{backgroundColor:bgColor,color:textColor}}> <div> <div className="text-xl font-bold mb-2">{title}</div> <div className="text-sm opacity-80">Imaginea nu este disponibilă încă</div> </div> </div>):(<> <Image src={imageSrc}alt={title}fill className="object-cover transform transition-transform duration-700 hover:scale-105" sizes="(max-width:768px)100vw,(max-width:1200px)50vw,33vw" onError={()=>setImageError(true)}/> <div className="absolute inset-0 opacity-70 transition-opacity duration-300 hover:opacity-80" style={{backgroundColor:bgColor}}/> </>)}</div>{ }<div className="relative z-10 h-full w-full p-6 flex flex-col justify-between"> <div> <span className="text-xs font-mono mb-2 block" style={{color:borderColor}}>{type==='vopsele'?'VOPSELE MODERNE':'IZOLAȚII ECO-FRIENDLY'}</span> <h3 className="text-2xl font-bold mb-3" style={{color:textColor}}>{title}</h3> <p className="mb-6 text-sm" style={{color:`${textColor}CC`}}>{description}</p> </div> <Link href={link}className="inline-flex items-center group" style={{color:textColor}}> <span className="mr-2">Află mai multe</span> <span className="w-6 h-6 rounded-full flex items-center justify-center transition-colors duration-300 group-hover:bg-white/20" style={{border:`1px solid ${borderColor}`}}> <svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http:> <path d="M1 6H11M11 6L6 1M11 6L6 11" stroke={textColor}strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/> </svg> </span> </Link> </div> </motion.div>);};export default ServiceCard;